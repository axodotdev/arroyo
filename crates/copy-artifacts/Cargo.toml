[package]
name = "copy-artifacts"
version = "0.12.0-dev"
edition = "2021"
publish = false

[dependencies]
arroyo-storage = { path = "../arroyo-storage" }
regex = "1"
tokio = {version = "1", features = ["fs", "rt", "net"]}
webpki = ">=0.22.2"
